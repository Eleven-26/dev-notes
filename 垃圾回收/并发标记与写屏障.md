# 并发标记与写屏障

> 并发标记**凭什么是对的**：三色抽象与漏标必须同时满足的两个条件、两类屏障（增量更新 / SATB）的正确性与工程代价。
> JVM 与 Go 收集器的取舍几乎都能从这里找到出处。
>
> 内容整理自语雀笔记，原书为《垃圾回收的算法与实现》（中村成洋 / 相川光）；三色标记在 JVM 的落地见 [JVM与垃圾回收.md](../java/JVM与垃圾回收.md)。
> 各算法的过程见 [标记类GC算法.md](标记类GC算法.md)、[复制与分代回收.md](复制与分代回收.md)、[引用计数与增量回收.md](引用计数与增量回收.md)。

## 一、三色抽象与并发标记的正确性

### 不变式：三色不只是三种颜色

白/灰/黑真正的价值在于它们定义了两个**可以在并发执行中被验证的局部性质**（不变式）：

- **强三色不变式**：任何黑对象都不存在直接指向白对象的引用。黑 = "我扫完了，我不会再看你"，所以黑指向谁，谁就必须至少是灰。
- **弱三色不变式**：允许黑直指白，但该白对象必须**存在一条从某个灰对象出发的可达路径**（且这条路径被屏障"保护"——删除它的尝试会被记账）。

一句话区分：**强不变式管的是"边"（不许出现黑→白），弱不变式管的是"路径"（出现了也别断后路）**。串行标记两个不变式天然同时成立；只有当 mutator 与收集器并发跑、双方都在改同一张引用图时，它们才成为需要花钱维护的约束。

### 漏标（消失的引用）发生的两个必要条件 ⭐

并发标记中"活对象被当垃圾回收"（漏标，mis-colour / lost marking），**必须同时满足**：

1. 某个**黑对象新增了一条指向白对象的引用**（且这条新边没被任何机制记下——黑对象已扫描完毕，不会再被重看）；
2. 某个**灰对象（或灰的下游）删除了通往该白对象的全部路径**（且这次删除没被记下——否则标记线程还能顺藤摸瓜找到它）。

```text
正常世界:   灰 G ──→ 白 W          W 迟早被 G 扫到, 安全
第一步:     黑 B ──→ 白 W (新增)   B 不会再被扫, 但路径还在, 仍安全
第二步:     灰 G ╳──→ 白 W (删除)  最后一条"会被看到的"路径也没了
结果:       W 及其整条下游子图对标记线程永远隐身 → 被当作垃圾清除
```

两个条件缺一不可，所以正确性策略就是**二选一地破坏它**：盯住"新增边"或盯住"删除边"。这就是强/弱三色两条路线的分水岭，也是上面那张屏障对比表的组织逻辑。

### 强三色路线（Dijkstra 插入屏障 / Shaston）

- 做法：拦截**条件 1**。mutator 往一个（已变黑的）对象里写入指向白对象的新引用时，屏障立刻把新值染灰（Dijkstra：目标为黑时一律保护新值；Shaston 更省：只有"目标是黑 **且** 新值是白"才触发慢路径，多数写入两次位测试就走人）。
- 正确性论据：黑永不指白 ⇒ 标记终结时（无灰）白必然不可达 ⇒ 清除安全。
- 难点在**新分配对象**：新对象天然是白，若被黑对象直接引用而颜色字段还未初始化，屏障可能读到错误的色（Dijkstra 原始方案对"引用倒置 / 黑指新白"力不从心）。工程解法是让新对象"出生即被保护"（生来灰/黑，或由分配屏障兜底）——代价转嫁给了分配路径。
- 优点：不保护"快照之后新变成垃圾"的对象，**浮动垃圾相对少**。

### 弱三色路线（SATB 快照）

- 做法：拦截**条件 2**。标记启动时把所有根直接引用的对象当"灰"入账——相当于给此刻的可达集拍了一张**快照**；此后 mutator 每删除一条引用（覆盖旧值），前写屏障把**旧值**记入线程局部的 SATB 缓冲区，标记线程异步消化、照常涂色。于是快照里"灰到白"的路径被删除时只是改道进了缓冲区，弱不变式始终成立。
- 正确性论据：标记完成的不是"堆现在的可达集"，而是 **"标记开始那一刻的可达集"**——凡快照时活着的必被标为活。
- 两个后果：(1) 不需要重扫黑对象，重新标记阶段只需消化缓冲区，暂停对"引用修改次数"不敏感（G1 选它正是为了停顿可预测）；(2) 快照后死掉的对象照样被当成活的——**浮动垃圾是这条路线的内生账单**，不是实现缺陷。

### 浮动垃圾：并发回收必然的代价

- **为什么必然**：并发标记的产出只能是"标记窗口内某一时点的可达集"的超集。正确性要求屏障**宁可多保（把垃圾当活的），不可少保（把活的当垃圾）**——少标一个活对象就是应用层的悬垂引用事故。多保的部分，就是浮动垃圾。停止型 GC 没这个问题，纯粹因为它拿的是"清除那一刻"的精确答案。
- **何时被收**：浮动垃圾在下一轮标记开始前就已不可达，下一轮正常回收即可带走它（在哪个区就随那个区的回收走）。因此它占用的是一个 GC 周期的内存，并发周期越长、存活集变动越剧烈，浮动垃圾越多——这是大堆上做长时间并发标记时内存水位反而升高的原因之一。

### 重新标记（remark / rescan）在两种方案里各在做什么

| 维度 | 增量更新（CMS，破条件 1） | SATB（G1，破条件 2） |
| --- | --- | --- |
| 并发期记账内容 | 字段被改动的**黑对象**（或卡） | 被删除的**旧引用值**（进缓冲区） |
| 重新标记的主要工作 | 以这些黑对象为根**重扫**其字段，补上漏掉的新边 | 消化残余缓冲区/标记栈，把剩余灰对象处理完 |
| 暂停与什么成正比 | 并发期引用修改的次数与对象大小 | 缓冲区残余量（对写引用频率不敏感） |
| 浮动垃圾 | 较少（新增边被跟踪） | 较多（快照把死对象也保了） |

两种方案在 JVM 里都还有一笔公共账：重新标记是 STW 的，且要做**根重新扫描**（线程栈、JNI 等）与**弱/软/虚引用处理**——这两项与引用修改频率无关，通常才是重新标记暂停的稳定构成。

---

## 二、屏障的工程代价

正确性讲完了，接下来算钱。屏障是"每次指针操作额外执行的一小段代码"，它的成本结构决定收集器选型。

### 读屏障 vs 写屏障：拦截"使用边"还是"增删边"

- **写屏障**拦在**引用图的增删**上（store 指令附近）。漏标的两个条件都发生在写入侧，所以**并发标记的正确性问题用写屏障就够**——这是它成为主流的原因：Java/C# 这类托管语言的字段写入全部可被 JIT 插桩，实现路径成熟。
- **读屏障**拦在**引用图的使用**上（load 之后、解引用之前）。它慢路径触发条件"读到了状态不对的引用"，天然适合**并发移动**：对象搬家后旧地址留在各处引用里，只有读取现场能拦截并修正。写屏障无法胜任——它不知道谁会来读。
- **前/后**之分：前写屏障（pre-barrier）在执行写入前看到**旧值**，适合"保护被删除的引用"（SATB、Steele、汤浅、增量更新记录被改对象）；后写屏障（post-barrier）在写入后执行，拿到的是**新值/目标对象**，适合分代记账（"这个老年对象现在引用了年轻对象"）。

| 维度 | 读屏障 | 写屏障 |
| --- | --- | --- |
| 触发点 | 每次加载引用字段 | 每次写入引用字段 |
| 适合解决 | 并发**移动**（指针修正/自恢复） | 并发**标记**正确性、分代记账 |
| 典型实现 | 测试指针色位（着色指针）、陷阱地址 | 置脏卡、SATB 缓冲入队、当场染灰 |
| 主要风险 | 读远多于写，热点路径全被打扰 | 写风暴时缓冲区积压，标记追不上分配 |
| 代表 | ZGC、（历史上）Azul C4 | G1/Shenandoah（SATB）、CMS（增量更新）、几乎所有分代收集器 |

### Brooks 指针：把"搬家转发"藏进对象头

```text
每个对象头额外留一个字 fwd("自转发指针"):
  正常时:   obj.fwd == &obj        // 指向自己
  被移动后: obj.fwd == &obj_new    // 指向新家
访问任意字段前都要先多 load 一次 fwd 再寻址(旧地址仍然可用, 只是多一跳)
写屏障的职责变成: 存引用前, 把"还指向旧地址"的值 unfold 成真实地址
```

- 妙处：移动对象后**不需要立刻更新全堆引用**，旧引用自动"穿针"到新家，于是复制/整理可以完全并发地进行（Shenandoah 的并发整理即建立在此类机制上，细节以其设计文档为准）。
- 开销是三笔账：每次字段访问多一次内存读取（读放大的税）；对象头膨胀一个字（空间税）；对象基址不再是自身、编译器难以证明别名关系与做预取（优化损失税）。
- 这条税太重，ZGC 在 JDK 11 用了 Brooks 指针后，JDK 13 起重构为把转发信息直接放进对象/页表，省掉常态下的一次间接——**"能不用自转发就不用"是并发整理演进的方向**。

### SATB 的账本：前屏障 + 并发标记缓冲区

SATB 的写屏障本体不贵（判断 + 把旧值塞进队列），贵在三样：

1. **缓冲区生命周期**：线程局部 buffer 写满要向全局队列加锁换新的，分配/归还/同步是持续负担；缓冲区本身占内存。
2. **生产消费速率失配**：mutator 改引用速度 > 标记线程消化速度时，标记进度落后、堆持续增长——G1 表现为并发标记"追不上"而被迫扩大堆或提前再标记（没有银弹，这是并发回收的容量规划问题）。
3. **浮动垃圾**（见上节）：本质也是 SATB 账本上的负债，用内存换暂停平稳。

### 着色指针：几乎零读开销，但钱付在了别处

把元数据（标记色、是否已修正等）编码进**指针自身的高位**，而不是对象头：

- 读侧检查只是"指针 & 掩码看某位是否置起"——一次寄存器级位测试，**不需要访问对象额外字段**，这就是"几乎零读开销"的由来。
- 前提相当苛刻：(1) 体系结构的虚拟地址有闲置高位可占用（如 x86-64 只用 48/57 位）；(2) 解引用前必须按位与（AND）抹掉色位才能当地址用，这个"解色"操作要编译器和硬件寻址路径配合；(3) **OS 与加载器必须保证**：对象真实地址永不碰到这些高位、页表能按位配合（ZGC 用"同一物理页映射多个虚拟地址"的三色页表技巧，直接改 Linux 内核内存管理来支持）。所以着色指针是"运行时 + 编译器 + OS"三方共建的机制，移植性最差、收益也最大。
- 它本身不消除读屏障，只是把读屏障的**热路径检查**变便宜，慢路径仍去修正指针/转发对象。

---

---

## 面试官会追问什么

- **并发标记为什么会漏标？** → 必须同时满足两个条件：黑对象新增了指向白对象的引用，且灰对象侧通往该白对象的路径被删除。屏障方案无非二选一：破坏"新增被漏"（增量更新 / 强三色）或破坏"删除无痕"（SATB / 弱三色）。
- **强三色和弱三色到底差在哪？** → 强三色禁止"黑→白"这条**边**存在；弱三色允许边存在但保住从灰出发的**路径**（记账进缓冲区）。前者要盯每次插入、后者要盯每次删除，重标记阶段的成本结构因此完全不同。
- **为什么并发标记一定有浮动垃圾？** → 标记产出的是"标记窗口内某时刻可达集"的**超集**；正确性要求屏障宁可多保护、绝不能少保护。多保的垃圾本轮不收、下轮回收 —— 这是"不停世界"的定价，不是 bug。
- **SATB 和增量更新在重新标记时各做什么？** → 增量更新以"被记录的黑对象"为根重扫字段，暂停随引用修改次数增长（CMS 的痛点）；SATB 只消化缓冲区剩余 + 重扫根，暂停对写引用频率不敏感（G1 停顿可预测的原因之一）。

## 关联

- [GC基础算法.md](GC基础算法.md) — 评价维度与横向选型总表
- [引用计数与增量回收.md](引用计数与增量回收.md) — 增量式回收与三色标记的基础定义
- [复制与分代回收.md](复制与分代回收.md) — 卡表（记忆集）与写屏障的配合
- [JVM与垃圾回收.md](../java/JVM与垃圾回收.md) — 增量更新（CMS）与 SATB（G1）的实际取舍
- [内存分配器.md](../go/运行时/内存分配器.md) — Go 的分配器与 GC 如何配合
