首先，介绍结构和算法之前先了解一个慨念，什么是时间复杂度及它的表示方法。

# 时间复杂度

时间复杂度是同一问题可用不同算法解决，而一个算法的质量优劣将影响到算法乃至程序的效率。算法分析的目的在于选择合适算法和改进算法。  
在计算机科学中，算法的时间复杂度是一个函数，它定性描述了该算法的运行时间。这是一个关于代表算法输入值的字符串的长度的函数。时间复杂度常用大O符号表述，不包括这个函数的低阶项和首项系数。  
不仅仅用于表示时间复杂度，也用于表示空间复杂度。

# 符合表示

O表示时间复杂度，O后面的括号中有一个函数，指明某个算法的耗时/耗空间与数据增长量之间的关系。n代表输入数据的量。

# 举例：

**O(1)：** 这是最低的时空复杂度，也就是耗时/耗空间与输入数据大小无关，无论输入数据增大多少倍，耗时/耗空间都不变。
哈希算法就是典型的O(1)时间复杂度，无论数据规模多大，都可以在一次计算后找到目标（不考虑冲突的话）；  
**O(n)：** 代表数据量增大几倍，耗时也增大几倍。比如常见的遍历算法；  
**O(n²)：** 代表数据量增大n倍时，耗时增大n的平方倍，这是比线性更高的时间复杂度。比如冒泡排序，就是典型的O(n²)
的算法，对n个数排序，需要扫描n×n次；  
**O(logn)：**
代表当数据增大n倍时，耗时增大logn倍（这里的log是以2为底的，比如，当数据增大256倍时，耗时只增大8倍，是比线性还要低的时间复杂度）。二分查找就是O(
logn)的算法，每找一次排除一半的可能，256个数据中查找只要找8次就可以找到目标；  
**O(nlogn)：** 代表当数据增大n倍时，耗时增加就是n乘以logn，当数据增大256倍时，耗时增大256*
8=2048倍。这个复杂度高于线性低于平方。归并排序就是O(nlogn)的时间复杂度。

# 常用数据结构和查找算法

因为查找算法的时间复杂度和数据的结构息息相关，所以这里一起介绍数据结构和查找算法

| 查找算法或数据结构   | 时间复杂度             | 稳定性  | 元素要求  | 说明                                                   |
|-------------|-------------------|------|-------|------------------------------------------------------|
| 哈希表查找       | O(1)              | 稳定   | 无序    | 数据量越大越不稳定，可用多次哈希提高效率（参考布隆过滤器）。可用做索引，MySQL中的一种索引结构是哈希 |
| 顺序查找        | O(n)              | 不稳定  | 无序或有序 | 按顺序比较每个元素，直到找到关键字为止                                  |
| 二分查找        | O(log n)          | 稳定   | 有序    | 也称折半查找，当待搜索的集合是相对静态的有序数据集时，此时使用二分查找是较好的选择            |
| 插值查找        | O(log 2 (log 2n)) | 稳定   | 有序    | 基于二分查找算法，可以提高查找效率                                    |
| 斐波那契查找      | O(log 2n)         | 稳定   | 有序    | 二分查找的一种提升算法                                          |
| 分块查找        | O(n²)             | 较稳定  | 有序    | 又称索引顺序查，是折半查找和顺序查找的一种改进方法                            |
| 二叉树         | O(log n)          | 一般稳定 | 树形    | 看树的深度和偏移量，有些树太高，已退化成线型                               |
| 平衡二叉树(AVL)  | O(log n)          | 稳定   | 树形    | 二叉树的改良版，所有节点的左右子树高度差不超过1，AVL树适合用于插入与删除次数比较少，但查找多的情况  |
| 红黑树         | O(log n)          | 稳定   | 树形    | 弱平衡二叉树，对于搜索，插入，删除操作较多的情况下，可用红黑树                      |
| B-树(B-Tree) | O(log n)          | 稳定   | 树形    | 读B树，不是B减树，- 只是一个连接符。多路平衡查找树，树形不一定是二叉的                |
| B+树(B+Tree) | O(log n)          | 稳定   | 树形    | B+树，B树的改良版。MySQL中的另一种索引结构就是B+Tree                    |

# 常用排序算法

| 排序算法 | 时间复杂度      |             |             | 空间复杂度    | 稳定性 | 复杂性 |
|------|------------|-------------|-------------|----------|-----|-----|
|      | 平均情况       | 最坏情况        | 最好情况        |          |
| 冒泡排序 | O(n²)      | O(n²)       | O(n)        | O(1)     | 稳定  | 简单  |
| 快速排序 | O(n log n) | O(n²)       | O(n log n)  | O(log n) | 不稳定 | 较复杂 |
| 插入排序 | O(n²)      | O(n²)       | O(n)        | O(1)     | 稳定  | 简单  |
| 希尔排序 | O(n log n) | O(n log² n) | O(n log² n) | O(1)     | 不稳定 | 较复杂 |
| 选择排序 | O(n²)      | O(n²)       | O(n²)       | O(1)     | 不稳定 | 简单  |
| 归并排序 | O(n log n) | O(n log n)  | O(n log n)  | O(n)     | 稳定  | 简单  |
| 堆排序  | O(n log n) | O(n log n)  | O(n log n)  | O(1)     | 不稳定 | 较复杂 |
| 计数排序 | O(n + k)   | O(n + k)    | O(n)        | O(k)     | 稳定  | 简单  |
| 桶排序  | O(n + k)   | O(n²)       | O(n + k)    | O(n + k) | 稳定  | 较复杂 |
| 基数排序 | O(n x k)   | O(n x k)    | O(n x k)    | O(n + k) | 稳定  | 复杂  |

