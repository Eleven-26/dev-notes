# Go 限流器

> 令牌桶模型、Limit/Burst 参数、平滑生成与突发处理
>
> 内容整理自大厂 Go 后端面试真题视频，参考资料与原始素材见 [素材清单](../../素材清单.md)。

---

## 一、如何用 Go 实现一个限流器？

**来源**：`p=55` 携程云计算工程师一面 · 时长 7分38秒
**考察意图**：考**令牌桶**这个限流模型的原理，以及**参数的实际含义**。

### 一、先说工程实践（加分点）

> **通常直接用现成的轮子**——第三方 Go 库或**官方标准库**，
> **没有必要重复造轮子**。
>
> Go 官方的是 **`golang.org/x/time/rate`**（令牌桶实现）。
>
> **但面试时必须能把原理讲清楚**——这才是这题的考点。

### 二、两个核心参数（必须解释准确）

| 参数 | 含义 |
|---|---|
| **`Limit`** | **速率**：每秒恢复多少个令牌 |
| **`Burst`** | **突发流量上限**，也就是**桶的容量**（能缓存的令牌数量） |

**用一组数字理解**：

```text
Limit = 10/s（每秒恢复 10 个）
Burst = 100 （桶最多装 100 个）

→ 如果没有任何流量，需要 10 秒才能把桶填满
→ 突发流量来了：这一秒可以处理 100 个请求（因为桶里有 100 个令牌）
→ 这 100 个处理完，又需要至少 10 秒才能重新填满
```

### 三、令牌桶的字段（看源码能对上的结构）

| 字段 | 作用 |
|---|---|
| **互斥锁** | 多个请求都要"取令牌"，都要**访问共享资源**（桶里还有多少令牌），所以要加锁 |
| **`limit`** | 每秒恢复速率 |
| **`burst`** | 桶容量（处理能力上限） |
| **`tokens`** | **当前令牌数量**，满足 `tokens <= burst` |
| **最后一次限流事件的时间** | **根据这个时间差来恢复令牌** |

**直观理解**：
- 桶的**高度**由 `Burst` 决定（令牌数量最多就这么多）；
- `Limit` 决定**每秒能补多少个**。

### 四、关键实现细节：令牌是"平滑生成"的，不是一次性填满

**一个常见的天真写法**：
> "每秒到点了，就往桶里加 10 个令牌。"

**为什么不行**：
> 如果 `Burst` 小于每秒恢复量（比如 Burst=5、Limit=10/s），
> **一次性填 10 个也放不下**——桶会溢出。

**标准库的做法：平滑生成**

```text
Burst = 10，Limit = 10/s
→ 每秒恢复 10 个 = 每 100ms 恢复 1 个
```

> **令牌是"按时间比例慢慢补"的，而不是攒到一秒末尾一次性给。**

### 五、突发流量场景（走一遍完整过程）

**场景**：桶里当前有 10 个令牌，`Limit = 10/s`，`Burst = 100`，突然来了 100 个请求。

```text
① 100 个请求打过来 → 瞬间把 10 个令牌消耗完（tokens: 10 → 0）
② 因为有互斥锁，100 个请求中只有 10 个通过（拿到令牌）
③ 剩下 90 个直接被拒绝
```

**这么做的目的**：
> **把超出服务器负载的流量直接拒掉，保护服务器**——
> 避免整个服务被打挂，导致**所有**流量都不可用（包括那些本来能正常处理的）。

**这就是"限流"的本质：宁可拒绝一部分，也要保住整体可用。**

### 六、令牌耗尽后的行为

**令牌为 0 之后**：

- 按平滑方式，**每 100ms 生成 1 个令牌**；
- 此时**限流器已不具备突发处理能力**——因为每 100ms 才 1 个，
  需要**整整 1 秒**才能把桶重新填满，**而且期间若有其他请求还会继续消耗令牌**；
- 后续请求只能**平滑地按每秒 10 个**放行。

**反过来，如果流量很闲**：

> 每秒 10 个用不完 → **令牌会持续累积**，最多累积到 `Burst` 为止。
> **之后若来突发流量，累积了多少就能支持多少**——
> 这就是 `Burst` 这个参数存在的意义。

### 七、面试答题结构

```text
① 模型：令牌桶（Token Bucket）
② 两个参数：Limit（恢复速率）、Burst（桶容量 / 突发上限）
③ 机制：请求先取令牌，取到才放行；令牌按时间平滑恢复
④ 突发：桶内令牌可被瞬间消耗，超出的请求直接拒绝（保护服务）
⑤ 工程实践：生产用 golang.org/x/time/rate，别自己造轮子
```

### 面试官会追问什么

- **令牌桶 vs 漏桶的区别？** → 令牌桶**允许突发**（桶里攒的令牌可以一次用掉）；
  漏桶**恒定速率流出**，不允许突发。所以令牌桶更适合互联网业务。
- **`Burst` 设太小会怎样？** → 允许多少并发就设多少；
  设成 1 就等于**严格串行**（退化成"每 100ms 才放一个"）。
- **怎么实现"每用户限流"？** → 按用户维度维护独立的限流器
  （`map[userID]*rate.Limiter`），注意**要清理不活跃用户，避免内存泄漏**。
- **`Wait` 和 `Allow` 的区别？** → `Wait` 会**阻塞等待**到能取到令牌；
  `Allow`/`AllowN` **立即返回**是否允许，适合 quick-reject 场景。

---

## 关联

- [../../分布式/限流降级熔断.md](../../分布式/限流降级熔断.md) — 限流 / 熔断 / 降级的整体视角
- [../../数据存储/redis/缓存问题与方案.md](../../数据存储/redis/缓存问题与方案.md) — 缓存层兜底与限流的配合
- [../工程实践/context.md](../工程实践/context.md) — 等待令牌时的取消
